{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch HTML in Chrome Dev",
            "type": "chrome",
            "request": "launch",
            "file": "${file}",
            "runtimeExecutable": [
                "dev"
            ]
        }
    ]
}